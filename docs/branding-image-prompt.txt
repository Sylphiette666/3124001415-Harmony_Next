Edit the supplied PNG by extracting ONLY the school emblem in the UPPER centered region. This is precise logo extraction for a HarmonyOS app icon, NOT a logo redesign. The source is 764 x 620 pixels and contains an important ALPHA transparency channel: ignore RGB values in fully transparent pixels (they look like solid red rectangles if alpha is ignored). The emblem occupies approximately x=192..571, y=0..377. The lower region y=398..619 is the school name lettering and MUST be excluded. Preserve the original emblem's exact red line art, all internal symbols and lettering, circle shape, and colors. Do not invent or redraw any detail. Make one square white-background app icon, 1024 x 1024, with the extracted complete circular emblem centered, filling about 70% of the width and height (15% even white margins each side). No shadow, no rounded outer mask (system will apply its own), no added elements. Strictly retain the emblem in the input and use its alpha to composite it onto white.